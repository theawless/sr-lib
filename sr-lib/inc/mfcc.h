#include <iostream>
#include <vector>
#include <string>
#include <iostream>
#include <vector>
#include <map>

using namespace std;

/// Gives back a representation of speech in terms of mfc coefficients.
std::vector<std::vector<double>> speech_to_coefficients(const std::vector<std::vector<double>> &segments);
